module test_struct_arr_minimal

test_struct_arr_minimal :: fun() {
    f : Foo
    print(f.x)   // should work: plain scalar field

    b : Bar
    print(b.xs[0])  // does this work?
}

Foo :: struct {
    x: i64
}

Bar :: struct {
    xs: [4]i64
}
