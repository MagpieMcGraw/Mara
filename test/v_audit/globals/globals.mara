module globals

// V audit: V claims to prevent globals but allows them via const + heap.
// Mara analog: probe module-scope decl + mutation.

counter : i64 = 0

bump :: fun() {
    counter = counter + 1
}

main :: fun() -> i64 {
    bump()
    return counter
}
