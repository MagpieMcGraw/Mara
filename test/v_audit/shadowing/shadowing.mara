module shadowing

// V audit: V correctly rejects same-scope redeclaration but allows
// closure parameters to shadow captures.
// Mara analog: try same-scope redeclaration.
// Expected behaviour: parse/type-check error — variable already declared.

main :: fun() -> i64 {
    x := 10
    x := 20
    return x
}
