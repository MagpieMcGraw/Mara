module uninit_read

// V audit: reading uninitialized array elements segfaults.
// Mara analog: declare a pointer without an initializer, then read it.
// Expected behaviour: type checker rejects via uninit_refs tracking
// ("used before being assigned a value").

main :: fun() -> i64 {
    p : ^i64
    return p^
}
