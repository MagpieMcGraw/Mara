module test_early_self

use mara.math

Thing :: struct(seed : f32) {
	c := helper(#self)
	a : f32 = 1.0
	b : f32 = 2.0
}

helper :: fun(t: ^Thing) -> f32 {
	return t.a + t.b
}

main :: fun() {
	t := Thing(0)
	print(t.c)
}
