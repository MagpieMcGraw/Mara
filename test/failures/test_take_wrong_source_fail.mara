module test_take_wrong_source_fail

// Should fail: let's storage must be a []byte slice, not some other type.

Pair :: struct {
    a: i64
    b: i64
}

run :: fun(nums: ^[]i64) {
    p : Pair = let(Pair, nums)
    p.a = 5
}

test_take_wrong_source_fail :: fun() {
}
